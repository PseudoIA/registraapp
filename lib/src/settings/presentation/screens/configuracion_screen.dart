// lib/src/features/settings/presentation/screens/configuracion_screen.dart
import 'package:flutter/material.dart';

class ConfiguracionScreen extends StatelessWidget {
  const ConfiguracionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pantalla de Configuración', style: TextStyle(fontSize: 24)),
    );
  }
}
